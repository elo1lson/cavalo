const horse = document.querySelector('img')

document.addEventListener('click',({target})=>{
console.log(target.classList.contains('quiz-button'));
if(target.classList.contains('quiz-button')){
  
}
})